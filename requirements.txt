# Core dependencies for simpleStream experiments
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.25.0

# Video processing
decord>=0.6.0
av>=12.0.0
qwen-vl-utils<0.0.12

# Vision & utilities
Pillow>=10.0.0
numpy>=1.24.0
tqdm>=4.65.0
torchvision>=0.16.0
matplotlib>=3.8.0

# Optional: flash attention (recommended for speed)
# pip install flash-attn --no-build-isolation
